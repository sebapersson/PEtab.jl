import{_ as a,c as t,o as s,aA as o}from"./chunks/framework.DjvtRZ2q.js";const m=JSON.parse('{"title":"Frequently Asked Questions","description":"","frontmatter":{},"headers":[],"relativePath":"FAQ.md","filePath":"FAQ.md","lastUpdated":null}'),i={name:"FAQ.md"};function r(n,e,l,h,d,p){return s(),t("div",null,[...e[0]||(e[0]=[o(`<h1 id="FAQ" tabindex="-1">Frequently Asked Questions <a class="header-anchor" href="#FAQ" aria-label="Permalink to &quot;Frequently Asked Questions {#FAQ}&quot;">​</a></h1><h2 id="install_fail" tabindex="-1">Why do I encounter installation problems? <a class="header-anchor" href="#install_fail" aria-label="Permalink to &quot;Why do I encounter installation problems? {#install_fail}&quot;">​</a></h2><p>PEtab.jl is regularly tested on, and should be installable on Linux, macOS and Windows. If you encounter installation issues on these systems, we recommend checking the following two common causes:</p><ol><li><p>an incorrectly installed or outdated Julia version,</p></li><li><p>outdated package dependencies.</p></li></ol><p>First, ensure that a supported Julia version is used. PEtab.jl is tested with Julia LTS version <strong>1.10</strong> and the latest stable version. Using an earlier version may result in installation failures. To reliably install and manage Julia versions across operating systems, we strongly recommend using <a href="https://github.com/JuliaLang/juliaup" target="_blank" rel="noreferrer">juliaup</a>. If you are constrained to using an older Julia version, for example on an HPC cluster, and encounter problems, please file an issue on <a href="https://github.com/sebapersson/PEtab.jl/issues" target="_blank" rel="noreferrer">GitHub</a>.</p><p>Second, installation failures may result from outdated versions of PEtab.jl dependencies. For example, if PEtab.jl is installed into the global Julia environment, older versions of other packages may prevent the latest version from being installed. To avoid this, it is recommended to install PEtab.jl in a new, isolated environment. For example, to install it in an environment named <code>petab_project</code>, run the following in the Julia REPL:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Pkg</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">activate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;petab_project&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PEtab&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Add any additional packages as needed</span></span></code></pre></div><p>If you need to install PEtab.jl into an existing environment and encounter issues, updating all packages may resolve the problem:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p>Lastly, if you have tried everything above and still experience installation issues, it is likely a bug in PEtab. In this case, please open an issue on <a href="https://github.com/sebapersson/PEtab.jl" target="_blank" rel="noreferrer">GitHub</a>.</p><h2 id="How-do-I-check-that-I-implemented-my-parameter-estimation-problem-correctly?" tabindex="-1">How do I check that I implemented my parameter estimation problem correctly? <a class="header-anchor" href="#How-do-I-check-that-I-implemented-my-parameter-estimation-problem-correctly?" aria-label="Permalink to &quot;How do I check that I implemented my parameter estimation problem correctly? {#How-do-I-check-that-I-implemented-my-parameter-estimation-problem-correctly?}&quot;">​</a></h2><p>After creating a <code>PEtabODEProblem</code>, it is important to check that everything works as expected. Since PEtab.jl creates parameter estimation problems, this means checking that the objective function (the problem likelihood) is computable, because if not, running parameter estimation will only return <code>NaN</code> or <code>Inf</code>.</p><p>The first step to verify that the likelihood is computable is to check if the objective function can be computed for the nominal parameters:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> get_x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(petab_prob)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">petab_prob</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nllh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)</span></span></code></pre></div><p>The nominal values can be specified when creating a <code>PEtabParameter</code> or in the parameters table if the problem is provided in the PEtab standard format (otherwise, they default to the mean of the parameter bounds). If the problem is correctly specified, the likelihood should be computable for these values. However, sometimes the nominal values can be poor choices (far from the &#39;true&#39; parameters as we do not know them, hence the need for PEtab.jl), and the code above may return <code>Inf</code> because the ODE cannot be solved. If this happens, check if the likelihood can be computed for random parameters:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">get_startguesses</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(petab_prob, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Specially, the <a href="/PEtab.jl/dev/API#PEtab.get_startguesses"><code>get_startguesses</code></a> function tries to find random parameter vectors for which the likelihood can be computed. If this function fails to return a parameter set, there is likely an issue with the problem formulation.</p><p>If the objective function cannot be computed, check out the tips below. If none of the suggestions help, please file an <a href="https://github.com/sebapersson/PEtab.jl/issues" target="_blank" rel="noreferrer">issue</a> on GitHub.</p><h2 id="Why-do-I-get-NaN-or-Inf-when-computing-the-objective-function-or-during-parameter-estimation?" tabindex="-1">Why do I get <code>NaN</code> or <code>Inf</code> when computing the objective function or during parameter estimation? <a class="header-anchor" href="#Why-do-I-get-NaN-or-Inf-when-computing-the-objective-function-or-during-parameter-estimation?" aria-label="Permalink to &quot;Why do I get \`NaN\` or \`Inf\` when computing the objective function or during parameter estimation? {#Why-do-I-get-NaN-or-Inf-when-computing-the-objective-function-or-during-parameter-estimation?}&quot;">​</a></h2><p>Sometimes, when computing the likelihood (<code>petab_prob.nllh(x)</code>) or during parameter estimation, <code>Inf</code> or <code>NaN</code> may be returned. This can be due to several reasons.</p><p><code>Inf</code> is returned when the ODE model cannot be solved. When this happens, a warning like <code>Failed to solve ODE model</code> should be printed. If no ODE solver warning is shown, check that the observable formulas and noise formulas cannot evaluate to <code>Inf</code> (e.g., there are no expressions that can evaluate to <code>log(0)</code>). If neither of these reasons causes <code>Inf</code> to be returned, please file an <a href="https://github.com/sebapersson/PEtab.jl/issues" target="_blank" rel="noreferrer">issue</a> on GitHub. For how to deal with ODE solver warnings, see one of the questions below.</p><p>If <code>NaN</code> is returned, the model formulas are likely ill-formulated. In PEtab.jl, the most common cause of <code>NaN</code> being returned is that <code>log</code> is applied to a negative value, often due to an ill-formulated noise formula. For example, consider the observable <code>h = PEtabObservable(:obs1, X, sigma * X)</code>, where <code>X</code> is a model species and <code>sigma</code> is a parameter. When computing the objective value (likelihood) for this observable, the <code>log</code> of the noise formula <code>sigma * X</code> is evaluated. Even if the model uses mass-action kinetics and <code>X</code> should never go below zero, in practice, numerical noise during ODE solving can cause <code>X</code> to become negative, leading to a negative argument for <code>log</code>. Therefore, a more stable noise formula than the one above would be <code>sigma1 + sigma2 * X</code>.</p><h2 id="Why-do-I-get-the-error-MethodError:-Cannot-convert-an-object-of-type-Nothing-to-an-object-of-type-Real?" tabindex="-1">Why do I get the error <em>MethodError: Cannot convert an object of type Nothing to an object of type Real</em>? <a class="header-anchor" href="#Why-do-I-get-the-error-MethodError:-Cannot-convert-an-object-of-type-Nothing-to-an-object-of-type-Real?" aria-label="Permalink to &quot;Why do I get the error _MethodError: Cannot convert an object of type Nothing to an object of type Real_? {#Why-do-I-get-the-error-MethodError:-Cannot-convert-an-object-of-type-Nothing-to-an-object-of-type-Real?}&quot;">​</a></h2><p>This error likely occurs because some model parameters have not been defined. For example, consider the observable <code>h = PEtabObservable(X, sigma)</code>, where <code>X</code> is a model species and <code>sigma</code> is a parameter. If <code>sigma</code> has not been defined as a <code>PEtabParameter</code>, the above error will be thrown when computing the objective function. This also applies to misspellings. For example, if the observable is defined as <code>h = PEtabObservable(X, sigma1)</code> but only <code>sigma</code> (not <code>sigma1</code>) is defined as a <code>PEtabParameter</code>, the same error will be thrown.</p><h2 id="Why-are-my-parameter-values-e.g.,-start-guesses-negative?" tabindex="-1">Why are my parameter values (e.g., start-guesses) negative? <a class="header-anchor" href="#Why-are-my-parameter-values-e.g.,-start-guesses-negative?" aria-label="Permalink to &quot;Why are my parameter values (e.g., start-guesses) negative? {#Why-are-my-parameter-values-e.g.,-start-guesses-negative?}&quot;">​</a></h2><p>When creating start-guesses for parameter estimation with <a href="/PEtab.jl/dev/API#PEtab.get_x"><code>get_x</code></a> or <a href="/PEtab.jl/dev/API#PEtab.get_startguesses"><code>get_startguesses</code></a>, the values in the returned vector(s) can sometimes be negative. As discussed in the <a href="/PEtab.jl/dev/tutorial#tutorial">starting tutorial</a>, this is because parameters are estimated on the <code>log10</code> scale by default, as this often improves performance. Consequently, when setting new parameter values manually, they should be provided on the parameter scale. It is also possible to change the parameter scale; see <a href="/PEtab.jl/dev/API#PEtab.PEtabParameter"><code>PEtabParameter</code></a>.</p><h2 id="I-get-ODE-solver-warnings-during-parameter-estimation,-is-my-model-wrong?" tabindex="-1">I get ODE-solver warnings during parameter estimation, is my model wrong? <a class="header-anchor" href="#I-get-ODE-solver-warnings-during-parameter-estimation,-is-my-model-wrong?" aria-label="Permalink to &quot;I get ODE-solver warnings during parameter estimation, is my model wrong? {#I-get-ODE-solver-warnings-during-parameter-estimation,-is-my-model-wrong?}&quot;">​</a></h2><p>When doing parameter estimation, it is not uncommon for the warning <code>Failed to solve ODE model</code> to be thrown a few times. This is because when estimating model parameters with a numerical optimization algorithm that starts from random points in parameter space (e.g., when using <a href="/PEtab.jl/dev/API#PEtab.calibrate_multistart"><code>calibrate_multistart</code></a>), poor parameter values that cause difficult dynamics to solve are sometimes generated. However, if the <code>Failed to solve ODE model</code> warning is thrown frequently, there is likely a problem with the model structure, or a suboptimal ODE solver is used. We recommend first checking if the issue is related to the ODE solver.</p><p>A great collection of tips for dealing with different ODE solver warnings can be found <a href="https://docs.sciml.ai/DiffEqDocs/stable/basics/faq/#faq" target="_blank" rel="noreferrer">here</a>. Briefly, it can be helpful to adjust the tolerances in <a href="/PEtab.jl/dev/API#PEtab.ODESolver"><code>ODESolver</code></a>, as the default settings are quite strict. Further, if <code>maxiters</code> warnings are thrown, increasing the number of maximum iterations might help. Lastly, it can also be worthwhile to try different ODE solvers. Even though the <a href="/PEtab.jl/dev/configuration/default_options#default_options">default solver</a> often performs well, every problem is unique. For hard-to-solve models, it can therefore be useful to try solvers like <code>Rodas5P</code>, <code>QNDF</code>, <code>TRBDF2</code>, or <code>KenCarp4</code>.</p><p>If changing ODE solver settings does not help, something may be wrong with the model structure (e.g., the model may not be coded correctly). However, it should also be kept in mind that some models are just hard to solve/simulate. Therefore, even if many warnings are thrown, a multi-start parameter estimation approach can still sometimes find a set of parameters that fits the data well.</p><h2 id="How-do-I-turn-off-ODE-solver-printing?" tabindex="-1">How do I turn off ODE solver printing? <a class="header-anchor" href="#How-do-I-turn-off-ODE-solver-printing?" aria-label="Permalink to &quot;How do I turn off ODE solver printing? {#How-do-I-turn-off-ODE-solver-printing?}&quot;">​</a></h2><p>When performing parameter estimation, as discussed above warnings are thrown when the ODE solver fails to solve the underlying ODE model. By default, we do not disable ODE solver warnings, as it can be beneficial to see them. In particular, if warnings are thrown frequently, it may indicate that something is wrong with the model structure (e.g. the model was not coded correctly) or that a sub-optimal (e.g., non-stiff) ODE solver was chosen when a stiff one should be used. Regardless, when running parameter estimation, it might be preferable not to have the terminal cluttered with warnings. You can turn off the warnings by setting the <code>verbose = false</code> option in the <code>ODESolver</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">osolver </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ODESolver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Rodas5P</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); verbose </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">petab_prob </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PEtabODEProblem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(model; odesolver </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> osolver)</span></span></code></pre></div><p>For which ODE solver to choose when manually setting the <code>ODESolver</code>, see <a href="/PEtab.jl/dev/configuration/default_options#default_options">this</a> page.</p><h2 id="How-do-I-create-a-parameter-estimation-problem-for-an-SBML-model?" tabindex="-1">How do I create a parameter estimation problem for an SBML model? <a class="header-anchor" href="#How-do-I-create-a-parameter-estimation-problem-for-an-SBML-model?" aria-label="Permalink to &quot;How do I create a parameter estimation problem for an SBML model? {#How-do-I-create-a-parameter-estimation-problem-for-an-SBML-model?}&quot;">​</a></h2><p>If your model is in the <a href="https://sbml.org/" target="_blank" rel="noreferrer">SBML</a> standard format, there are two ways to create a parameter estimation problem:</p><ol><li><p>Formulate the problem in the <a href="https://petab.readthedocs.io/en/latest/" target="_blank" rel="noreferrer">PEtab</a> standard format (recommended). PEtab is a standard format for parameter estimation that assumes the model is in the SBML format. We recommend creating problems in this format, as it allows for the exchange of parameter estimation workflows and is more reproducible. A guide on how to create problems in this standard format can be found <a href="https://petab.readthedocs.io/en/latest/" target="_blank" rel="noreferrer">here</a>, and a tutorial on importing problems can be found <a href="/PEtab.jl/dev/tutorials/define_problem/standard_format#import_petab_problem">here</a>.</p></li><li><p>Import the model as a <a href="https://github.com/SciML/Catalyst.jl" target="_blank" rel="noreferrer">Catalyst.jl</a> <code>ReactionSystem</code> with <a href="https://github.com/sebapersson/SBMLImporter.jl" target="_blank" rel="noreferrer">SBMLImporter.jl</a> (see the SBMLImporter <a href="https://sebapersson.github.io/SBMLImporter.jl/stable/" target="_blank" rel="noreferrer">documentation</a> for details). As demonstrated in the starting <a href="/PEtab.jl/dev/tutorial#tutorial">tutorial</a>, a <code>ReactionSystem</code> is one of the model formats that PEtab.jl accepts for creating a parameter estimation problem directly in Julia.</p></li></ol>`,37)])])}const u=a(i,[["render",r]]);export{m as __pageData,u as default};
